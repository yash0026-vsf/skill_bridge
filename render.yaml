services:
  - type: web
    name: statskill-backend
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn statskill_backend:app --host 0.0.0.0 --port 
    envVars:
      - key: GEMINI_API_KEY
        sync: false
